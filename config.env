# Environment Variables for Exam System Backend

# Server Configuration
PORT=3000
NODE_ENV=development # development or production

# Database (MongoDB Atlas example)
# Replace <PASSWORD> with your actual database user password
# Replace myFirstDatabase with your actual database name
DATABASE_URL="mongodb://127.0.0.1:27017/examSystemDB"
DATABASE_PASSWORD=<YOUR_DB_PASSWORD>

# JWT Configuration
JWT_SECRET=a-very-secure-secret-key-for-jwt-please-change-this
JWT_EXPIRES_IN=90d # e.g., 90d, 1h, 30m
# JWT_COOKIE_EXPIRES_IN=90 # Days (if using cookie-based JWT)

# CORS Origin (Optional - defaults to all if not set or using basic cors())
# FRONTEND_URL=http://localhost:4200 # Your Angular app's URL

